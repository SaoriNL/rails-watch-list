<div class="container">
  
  <% @lists.each do |list| %>
    <p><%= list.name %></p>
  <% end %>

   <%# <%= link_to new_monster_curse_path(monster), class: 'card-add-link' do %>
   <%= link_to 'Add List', new_list_path, class: 'btn btn-primary mt-2' %>
   <%# <%= link_to 'Add Bookmark', new_list_bookmark_path(@list), class: 'btn btn-primary mt-2' %>
   <%# <%= link_to 'List Bookmarks', list_bookmarks_path(@lists), class: 'btn btn-primary mt-2' %>
  <%# <%= cl_image_tag("nuiyxj6yximtfmndw4dd",
      width: 400, height: 300, crop: :fill) %>

<%# <%= cl_image_tag @list.photo.key, height:300, width:400 %>


</div>
